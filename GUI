package donnee;

import java.awt.event.ActionEvent;
import java.awt.event.ActionListener;

import javax.swing.JButton;
import javax.swing.JFrame;
import javax.swing.JLabel;

public class GUI extends JFrame{
	
	protected JLabel nomEntreprise= new JLabel("Nom Entreprise : ");
	protected JLabel FavoriEntreprise= new JLabel("Entreprise Favorite : ");
	protected JLabel ListeEntreprise= new JLabel("Affichage Entreprise : ");
	protected JLabel Capital=new JLabel("Affiche Capital");
	
	protected JButton ajouterFavori;
	protected JButton rechercherNom;
	protected JButton ListEntreprise;
	protected JButton rechercherCapital;
	
	
	private final InterfaceEntreprise entreprise;
	
	public GUI (String title) {
		super(title);
		entreprise=new ListEntreprise();
	}
	
	protected void Actions(){
		ajouterFavori.addActionListener(new AjouterEntreprise());
		rechercherNom.addActionListener(new RechercherNomAction());
		ListEntreprise.addActionListener(new ListAction());
		rechercherCapital.addActionListener(new RechercherCapitalAction());
	}
	
	private class AjouterEntreprise implements ActionListener{
		
		public void ActionPerformed(ActionEvent e) {
			String nom=
			
		}
		
		
		
	}

}
